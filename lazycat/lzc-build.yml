# Consumed by `lzc-cli project build` (run from this directory). The image
# is built/pushed by GitHub Actions; here we only point lzc-cli at the
# rendered manifest + icon so it can package the lpk.
pkgout: ./
icon: ./icon.png
manifest: ./lzc-manifest.yml
package: ./package.yml
